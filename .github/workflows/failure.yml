name: CI

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]
    branches:
      - master 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      
      - name: Early exit
        run: |
              echo 'hey'
              exit 1
                        
      - name: another job
        run: |
              echo 'another text'
