name: Triggers Twice

on:
  pull_request:
    types: [edited]
    branches:
    - master
  pull_request_review:
    branches:
    - master

jobs:
  TriggerTwice:
    runs-on: ubuntu-latest
    steps:
      
      - name: Echo a line
        run: |
              echo 'hello'
